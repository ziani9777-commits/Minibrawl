if (screen === "menu") {
    const rank = getRank(save.t);
    const b = BRAWLERS[save.sel];

    return (
      <div style={{
        width: "100vw", 
        height: "100vh",
        background: "radial-gradient(circle at 50% 50%, #2980b9 0%, #2c3e50 100%)", // Fond bleu dynamique
        fontFamily: "'Arial Black', Arial, sans-serif",
        color: "#fff", 
        position: "relative", 
        overflow: "hidden",
        userSelect: "none" // Empêche la sélection de texte
      }}>
        
        {/* Barre du Haut : Profil et Trophées */}
        <div style={{ position: "absolute", top: 20, left: 20, display: "flex", gap: 15 }}>
          {/* Icône de Rang */}
          <div style={{
            background: rank.c, border: "4px solid #000", borderRadius: 12, 
            width: 60, height: 60, display: "flex", alignItems: "center", justifyContent: "center",
            fontSize: 30, boxShadow: "0 4px 0 #000"
          }}>
            🏆
          </div>
          {/* Infos Joueur */}
          <div style={{ display: "flex", flexDirection: "column", justifyContent: "center" }}>
            <div style={{ fontSize: 24, fontWeight: 900, color: "#fff", textShadow: "2px 2px 0 #000", textTransform: "uppercase" }}>
              {rank.name}
            </div>
            <div style={{ fontSize: 18, color: "#FFD700", textShadow: "2px 2px 0 #000", fontWeight: 900 }}>
              {save.t} Trophées
            </div>
          </div>
        </div>

        {/* Ressources (Fausses pièces et gemmes pour le style) */}
        <div style={{ position: "absolute", top: 20, right: 20, display: "flex", gap: 10 }}>
          <div style={{ background: "rgba(0,0,0,0.6)", border: "3px solid #000", borderRadius: 20, padding: "4px 16px", display: "flex", alignItems: "center", gap: 8, fontSize: 18, fontWeight: 900 }}>
            <span>🪙</span> <span style={{ color: "#FFD700" }}>1250</span>
          </div>
          <div style={{ background: "rgba(0,0,0,0.6)", border: "3px solid #000", borderRadius: 20, padding: "4px 16px", display: "flex", alignItems: "center", gap: 8, fontSize: 18, fontWeight: 900 }}>
            <span>💎</span> <span style={{ color: "#2ecc71" }}>30</span>
          </div>
        </div>

        {/* Centre : Le Brawler Actif */}
        <div style={{
          position: "absolute", top: "50%", left: "50%", transform: "translate(-50%, -50%)",
          display: "flex", flexDirection: "column", alignItems: "center", zIndex: 10
        }}>
          {/* Emoji géant en guise de Brawler 3D */}
          <div style={{ 
            fontSize: 160, 
            filter: "drop-shadow(0px 15px 5px rgba(0,0,0,0.5))", 
            marginBottom: -10 
          }}>
            {["🔥","💪","⚡","❄️","👻"][b.id]}
          </div>
          
          {/* Nom du Brawler */}
          <div style={{ 
            fontSize: 50, fontWeight: 900, color: b.color, textTransform: "uppercase",
            WebkitTextStroke: "2px #000", filter: "drop-shadow(0px 4px 0px #000)"
          }}>
            {b.name}
          </div>
          
          <div style={{ background: "#e67e22", border: "3px solid #000", borderRadius: 20, padding: "4px 16px", fontSize: 16, fontWeight: 900, marginTop: 5, boxShadow: "0 4px 0 #000" }}>
            POUVOIR 9
          </div>
        </div>

        {/* Menu Gauche : Sélection Brawlers */}
        <div style={{ position: "absolute", bottom: 30, left: 20 }}>
          <button style={{
            background: "#3498db", border: "5px solid #000", borderRadius: 16, padding: "16px 24px",
            color: "#fff", fontSize: 24, fontWeight: 900, cursor: "pointer",
            boxShadow: "0 8px 0 #000", textTransform: "uppercase", letterSpacing: 1,
            transition: "transform 0.1s"
          }} 
          onPointerDown={(e) => e.currentTarget.style.transform = "translateY(4px)"}
          onPointerUp={(e) => e.currentTarget.style.transform = "translateY(0)"}
          onClick={() => setScreen("brawlers")}>
            👊 Brawlers
          </button>
        </div>

        {/* Menu Droite : Bouton JOUER Géant */}
        <div style={{ position: "absolute", bottom: 30, right: 30 }}>
          <button style={{
            background: "#f1c40f", border: "6px solid #000", borderRadius: 20, padding: "20px 60px",
            color: "#000", fontSize: 48, fontWeight: 900, cursor: "pointer",
            boxShadow: "0 10px 0 #d35400, 0 15px 20px rgba(0,0,0,0.5)", 
            textTransform: "uppercase", transform: "skewX(-5deg)", letterSpacing: 2
          }} 
          onPointerDown={(e) => e.currentTarget.style.transform = "skewX(-5deg) translateY(6px)"}
          onPointerUp={(e) => e.currentTarget.style.transform = "skewX(-5deg) translateY(0)"}
          onClick={() => setScreen("diffsel")}>
            JOUER
          </button>
        </div>

      </div>
    );
  }
